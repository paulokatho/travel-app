name: Java CI

on:
    push:
        branches: [ "feat/dev" ]

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v3

